import state from './state'
const getters = {
  // audio(state) {
  //   return state.audio;
  // },
  isShowIndex(state) {
    return state.isShowIndex;
  },
  musicData(state) {
    return state.musicData;
  }
};

export default getters;
